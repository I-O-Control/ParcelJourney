# Topology inventory

`fiege-ungarn-logical-topology.json` is the first conservative topology seed
derived from the ZAP17 archive. It contains logical equipment and tag mappings,
not physical coordinates. No coordinates are invented.

The next required artifact for a visual topology is a TIA Portal screen export
(PDF/SVG/PNG) or an engineering export containing object positions and links.
